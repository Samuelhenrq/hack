let app;_a07‍.w("./src/app",[["default",["app"],function(v){app=v}]]);

const PORT = 3000;

const server = app.listen(PORT, "localhost", ()=>{
    console.log(`Server running ${server.address().port}`);
});